============
Installation
============

At the command line::

    $ pip install nova-mksproxy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nova-mksproxy
    $ pip install nova-mksproxy
